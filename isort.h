#define SIZE 50

//The function will get a pointer to the array (not necessarily to the beginning of the array) and a number. The function will move 
//Next in one cell array to the right. Note that the function assumes that there is memory for at least 1 + i cells after
//The pointer to the array.
void shift_element(int* arr, int i);
//sort 
void insertion_sort(int* arr , int len);
